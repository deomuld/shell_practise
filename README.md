# shell_practise 

Day-1 ------>  count_files.sh


Day-2 ------>  Create a Bash script named find_error.sh that:
                Takes a filename as a command-line argument
                Checks whether the file exists
                Counts how many lines contain the word ERROR
                Prints:
                "File does not exist" if the file is invalid
                Otherwise: "ERROR lines: X"